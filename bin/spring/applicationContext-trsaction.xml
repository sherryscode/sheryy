<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:aop="http://www.springframework.org/schema/aop"
       xmlns:tx="http://www.springframework.org/schema/tx"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
    http://www.springframework.org/schema/beans/spring-beans.xsd
    http://www.springframework.org/schema/context
    http://www.springframework.org/schema/context/spring-context.xsd
    http://www.springframework.org/schema/aop
    http://www.springframework.org/schema/aop/spring-aop.xsd
    http://www.springframework.org/schema/tx
    http://www.springframework.org/schema/tx/spring-tx.xsd">

    <!--事务控制器-->
    <bean id="transactionManager" class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
        <property name="dataSource" ref="dataSource" />
        <!-- 设置事务超时时间，避免长时间占用连接 -->
        <property name="defaultTimeout" value="30"/>
    </bean>

    <!--通知-->
    <tx:advice id="txAdvice" transaction-manager="transactionManager">
        <tx:attributes>
            <!--传播行为，设置超时时间避免事务过长-->
            <tx:method name="save*" propagation="REQUIRED" timeout="30"/>
            <tx:method name="delete*" propagation="REQUIRED" timeout="30"/>
            <tx:method name="insert*" propagation="REQUIRED" timeout="30"/>
            <tx:method name="update*" propagation="REQUIRED" timeout="30"/>
            <tx:method name="select*" propagation="REQUIRED" timeout="30"/>
            <tx:method name="find*" propagation="SUPPORTS" read-only="true" timeout="10"/>
            <tx:method name="get*" propagation="SUPPORTS" read-only="true" timeout="10"/>
            <tx:method name="query*" propagation="SUPPORTS" read-only="true" timeout="10"/>
            <tx:method name="*" propagation="SUPPORTS" read-only="true" timeout="10"/>
        </tx:attributes>
    </tx:advice>

    <!--AOP-->
    <aop:config>
        <!--设置切入点-->
        <aop:pointcut id="serviceMethods" expression="execution(* com.system.service.impl.*.*(..))"/>
        <aop:advisor advice-ref="txAdvice" pointcut-ref="serviceMethods"/>
    </aop:config>

</beans>